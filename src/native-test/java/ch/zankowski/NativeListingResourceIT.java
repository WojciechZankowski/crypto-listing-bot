package ch.zankowski;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeListingResourceIT extends ListingResourceTest {

    // Execute the same tests but in native mode.
}
